PIXABAY_API_KEY=

# MySQL is the default database. Keep production credentials outside Git.
DB_ENGINE=mysql
DB_NAME=videomaker
DB_USER=videomaker
DB_PASSWORD=change-me
DB_HOST=127.0.0.1
DB_PORT=3306
DB_CONN_MAX_AGE=60

# Use this only when exporting data from the old local SQLite database.
# DB_ENGINE=sqlite
